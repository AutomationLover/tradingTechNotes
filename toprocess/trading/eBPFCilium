

我现在在 AWS EKS 上使用 Cilium 作为 CNI，在这个过程中，其实是借着 Cilium 这个项目系统地了解了一轮 eBPF。作为使用者，我虽然不自己写 eBPF C 程序，但因为要准备排查网络问题，所以刻意去理解了它在内核里的工作原理和关键技术点。

第一块是对 eBPF 程序的生命周期有了比较完整的认识。比如 Cilium 的 `cilium-agent` 会根据集群配置和内核版本，自动生成和编译 eBPF 程序，然后通过内核的加载接口把字节码丢给内核。内核这边会经过 Verifier 做严格检查，保证没有越界访问、死循环之类的问题，验证通过之后再由内核 JIT 编译成对应架构的机器码挂载到合适的 hook 上。这个流程让我比较直观地理解了：eBPF 是运行在内核里的“受控、安全插件”，而不是随便往内核里塞代码。

第二块是对内核里挂载点（hook）的理解。通过 Cilium，我知道它主要是在 TC（Traffic Control）这一层挂 eBPF 程序，在容器 veth 上做 L3/L4 转发和策略，等于是绕过了传统 iptables 链式匹配的部分开销。在一些配置里还可以用到 XDP，在驱动层更早地处理或丢弃报文，用来做高性能的流控或者防御。这些 hook 让我把 “数据包在内核的路径” 和 “eBPF 程序在哪个阶段介入” 串了起来。

第三块是对 BPF Map 和 Helper 的理解。Cilium 把很多状态，比如 endpoint identity、策略、负载均衡后端等，都存放在 BPF Map 里，用户态的 `cilium-agent` 负责更新这些 Map，内核态的 eBPF 程序在转发/过滤的时候直接查 Map 做决策。我也大概了解了 eBPF 不能随便调用内核函数，而是通过内核提供的 Helper 函数来安全地操作 skb、改写报文或者查询元数据。这些概念对我后面用 `cilium bpf` / `bpftool` 去排查问题非常重要。

最后一块是结合 Cilium 的“使用体验”去理解 eBPF 带来的价值。比如在大规模 EKS 集群里，Cilium 用 eBPF 和哈希表查找实现负载均衡，替代了 kube-proxy 的 iptables 规则链，这让服务数量上来之后延迟和 CPU 的增长更平滑；另外 Cilium 用 Identity 而不是 Pod IP 做安全策略匹配，identity 也是通过 BPF Map 在内核里做高效查找的。这些都让我更清楚 eBPF 在生产环境里是怎么提升性能和安全的。

所以总结一下：我通过在 EKS 上使用 Cilium，不只是学会了“怎么部署一个 CNI 插件”，而是刻意去理解了背后 eBPF 程序是如何被生成、验证、加载和挂载的，掌握了 BPF Map / Helper / 各种 hook 的基本概念，并且能在需要排障的时候，结合 Cilium CLI 和内核 eBPF 工具去定位问题。这一块也是我在准备和 eBPF 相关面试时重点整理的技术点。
